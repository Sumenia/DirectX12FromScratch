#include <iostream>
#include <time.h>
#include "SoundEngine.hh"
#include "Sound.hh"

#define DEBUG(s) std::cout << s << std::endl;

int main(int ac, char **av)
{
	if (ac < 2)
		return (0);
	return (0);
}